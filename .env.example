# Environment Variables Template
# Copy this to .env for local development
# Set these in Railway Dashboard for production

# Django Settings
SECRET_KEY=your-secret-key-here-generate-with-django-command
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (Railway auto-provides this in production)
DATABASE_URL=sqlite:///db.sqlite3

# Optional: Email configuration (for contact forms, password reset)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password

# Optional: Cloud storage for media files (recommended for production)
# AWS_ACCESS_KEY_ID=your-aws-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret
# AWS_STORAGE_BUCKET_NAME=your-bucket-name
# AWS_S3_REGION_NAME=us-east-1
